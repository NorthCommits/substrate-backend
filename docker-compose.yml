version: "3.9"

services:
  api:
    build: .
    ports:
      - "8001:8000"
    env_file:
      - .env
    environment:
      - APP_ENV=development
    volumes:
      - .:/app
    restart: unless-stopped